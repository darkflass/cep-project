▶ How to Run Locally

Because videos and dark mode use browser storage, you must use a local server.

Step 1 — Open terminal in project folder

Then run:

python -m http.server


or

python3 -m http.server

Step 2 — Open in browser
http://localhost:8000


Do NOT open files using file:// — that will break videos and dark mode.